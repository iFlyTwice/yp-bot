/**
 * Guild Manager Routes
 * This file serves as an entry point to the modularized guild manager routes
 */

const guildManagerRouter = require("./guild-manager/index");

module.exports = guildManagerRouter;
